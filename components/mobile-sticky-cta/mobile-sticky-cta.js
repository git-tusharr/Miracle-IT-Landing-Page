/**
 * MOBILE STICKY CTA CONTROLLER
 */
function initMobileStickyCta(container = document) {
  // Mobile sticky click tracking is bound via initCTATracking()
}

if (typeof window !== 'undefined') {
  window.initMobileStickyCta = initMobileStickyCta;
}
